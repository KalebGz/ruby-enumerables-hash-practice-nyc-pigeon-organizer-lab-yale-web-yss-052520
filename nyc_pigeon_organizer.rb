def nyc_pigeon_organizer(data)
  # write your code here!
  
  # Create an array of all the names found
  res = fata.each_with_object({}){ |key, value|, final_array
    
    value.each{|inner_key, names|
      names.each{ |name|
        if !final_array[name]
          final_hash[name] = {}
        end
      }
    }
  }
end
